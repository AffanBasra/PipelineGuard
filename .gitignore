.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
# Encoder weights and exported ONNX graphs (tracked via HF, not git).
# The comment is on its own line: git treats a trailing "#" as part of the
# pattern, not as a comment, so "models/ # ..." silently matched nothing.
models/
.idea/
graphify-out/
CLAUDE.md

.coverage
.coverage.*
htmlcov/
build/
dist/

data/nemotron-pii/
# OSM address data: ODbL-licensed, and the raw responses carry name and phone
# tags that docs/decisions.md section 1 forbids storing. Regenerate with
# scripts/fetch_osm_addresses.py.
data/osm-addresses/
